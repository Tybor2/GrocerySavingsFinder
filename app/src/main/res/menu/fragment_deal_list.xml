<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/refresh"
        android:title="Refresh Deals"
        app:showAsAction="never"/>

    <item
        android:id="@+id/barcode_search"
        android:title="Search Barcode"
        android:icon="@drawable/barcodeicon"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/menu_item_search"
        android:title="Search"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/menu_item_clear"
        android:title="Clear Search"
        app:showAsAction="never"/>

    <item
        android:id="@+id/filter"
        android:title="Filter"
        app:showAsAction="never" >

        <menu>

            <item
                android:id="@+id/filter_ht"
                android:title="Harris Teeter"/>

            <item
                android:id="@+id/filter_publix"
                android:title="Publix"/>
        </menu>

    </item>




</menu>